You will produce a large redo of the current robot local state code. This involves reworking the LiDAR and the IMU software to better localise the robot. The robot will operate autonomously on the field, and requires to be 'localised' at all times with as little latency as possible. Although the LiDAR can be used once per revolution, every 120ms, instead, as the angle of the robot is always known, the LiDAR should search for the field corner's and use the robot's average current heading over scanning the corner to localise itself. however, as the robot is constantly moving, each point gathered by the LiDAR needs to be subtracted by the robot's current rotation immediately to always remain localised even while rotating. additionally, the robot's position should be estimated at all times, so if for example the robot runs a moveTo() command, it predicts where it is supposed to be and corrects this when the LiDAR updates. do not use OLD lidar poses in moving to a location, it should only use the most recent pose. this is such that the robot should remain fully localised with no fluctuations in apparent position while moving. the current code does not work well, as even though it can localise after a revolution, it is too slow and when the robot starts moving, it immediately dies and makes the robot drive crazy. The code must be extremely robust, fast and concise. Keep unnecessary components to a minimum and make no mistakes. You are a master coder, and all creations should be extremely well thought out and flawless. Ensure that the robot can rotate and translate at high speeds and maintain an approximate pose, which importantly, does not cause issues with movement.